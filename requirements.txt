# Core Application
gradio>=4.0.0
python-dotenv>=1.0.0

# Machine Learning & Audio
# heartlib pins torch==2.4.1, so we let it handle torch version
numpy
scipy
soundfile

# Model Downloading
huggingface_hub>=0.20.0
hf_xet  # Xet Storage for faster HuggingFace downloads

# Model Runtime - handled by heartlib
# transformers, accelerate, torchtune, tokenizers are dependencies of heartlib

# HeartMuLa library (install from source - REQUIRES PYTHON 3.10)
# Install with: pip install "git+https://github.com/HeartMuLa/heartlib.git"
# Note: May need to temporarily disable Norton antivirus during install
